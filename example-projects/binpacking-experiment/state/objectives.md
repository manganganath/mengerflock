# Research Objectives

1. **Reduce total bins used** — close the gap to the lower bound (currently 29 bins across 13 holdout instances)
2. **Maintain fast runtime** — solver must complete within the 30s timeout per instance
