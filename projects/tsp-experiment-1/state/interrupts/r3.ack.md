# Urgent Redirect: r3

Directed FirstNode approaches consistently cause butterfly-effect regressions on
d2103/pcb3038 due to changing the Random() call sequence. This direction is exhausted.

**NEW DIRECTION**: Focus on KSwapKick improvements that don't change the random
number sequence for non-kick code paths.

Key insight: The current KSwapKick selects K=4 cut points. The reconnection pattern
(shift-by-2) is FIXED. This means every kick produces the same TYPE of perturbation.

Try these self-contained changes inside KSwapKick.c only:
1. **Longer walks for large instances**: Scale WALK_STEPS with Dimension. BUT keep
   the same number of Random() calls by padding with dummy calls if walk terminates
   early. This avoids butterfly effect in subsequent code.
2. **Better LongEdgeNode**: Instead of HUNT_COUNT random nodes, check ALL candidate
   edges of the current best tour and find the one with max C(N,Suc)-C(N,nearest).
   This is deterministic and doesn't consume Random() calls.
3. **Minimum spacing between cut points**: After selecting and sorting s[], verify
   adjacent cut points are at least Dimension/(2K) apart in tour order. If not,
   reject and re-kick. This avoids clustered weak perturbations.

IMPORTANT: d2103 is the ONLY remaining target. fl1577 and pcb3038 are already
optimal on current main. Any change must not regress those.
